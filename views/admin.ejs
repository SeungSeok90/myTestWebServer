<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" type="text/css" href="/css/style.css">
</head>
<body>
    <header class="bg-dark text-white text-center py-3">
        <div class="container">
            <div class="d-flex justify-content-between align-items-center">
                <h1>행사명</h1>
                <nav>
                    <a href="/" class="text-white">홈페이지</a> |
                    <a href="/login" class="text-white">로그인</a>
                </nav>
            </div>
        </div>
    </header>

    <div class="container-fluid">
        <div class="row">
            <nav class="col-md-2 d-none d-md-block bg-light sidebar" id="sidebar">
                <div class="sidebar-sticky">
                    <button class="btn btn-secondary btn-block" id="sidebarToggle">사이드바 토글</button>
                    <ul class="nav flex-column mt-2">
                        <li class="nav-item">
                            <a class="nav-link active" href="#">등록 현황</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="#">설문 현황</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="#">퀴즈 현황</a>
                        </li>
                    </ul>
                </div>
            </nav>

            <main role="main" class="col-md-9 ml-sm-auto col-lg-10 px-4">
                <h2 class="mt-4">대시보드</h2>
                <div class="dashboard">
                    <!-- 대시보드 내용 -->
                </div>
                
                <h2 class="mt-4 d-flex justify-content-between">
                    List
                    <a href="/admin/export" class="btn btn-success">내보내기</a>
                </h2>
                <div class="list">
                    <table class="table table-striped">
                        <thead>
                            <tr>
                                <th>이름</th>
                                <th>회사</th>
                                <th>부서</th>
                                <th>직급</th>
                                <th>이메일</th>
                                <th>전화번호</th>
                                <th>참석 여부</th>
                                <th>기념품</th>
                                <th>개인정보 수집 동의</th>
                                <th>개인정보 제3자 제공 동의</th>
                                <th>등록 일시</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% if (registrations && registrations.length > 0) { %>
                                <% registrations.forEach(registration => { %>
                                <tr>
                                    <td><%= registration.name %></td>
                                    <td><%= registration.company %></td>
                                    <td><%= registration.department %></td>
                                    <td><%= registration.position %></td>
                                    <td><%= registration.email %></td>
                                    <td><%= registration.phone %></td>
                                    <td><%= registration.attendance %></td>
                                    <td><%= registration.gift %></td>
                                    <td><%= registration.agreement1 ? 'Yes' : 'No' %></td>
                                    <td><%= registration.agreement2 ? 'Yes' : 'No' %></td>
                                    <td><%= registration.created_at %></td>
                                </tr>
                                <% }); %>
                            <% } else { %>
                                <tr>
                                    <td colspan="11" class="text-center">등록된 인원이 없습니다.</td>
                                </tr>
                            <% } %>
                        </tbody>
                    </table>
                </div>
            </main>
        </div>
    </div>

    <footer class="bg-dark text-white text-center py-4">
        <p class="mb-0">Copyright © 2024 MegazoneCloud Corp. All rights reserved.</p>
    </footer>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.4/dist/umd/popper.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    <script>
        document.getElementById('sidebarToggle').addEventListener('click', function() {
            const sidebar = document.getElementById('sidebar');
            sidebar.classList.toggle('d-none');
        });
    </script>
</body>
</html>
